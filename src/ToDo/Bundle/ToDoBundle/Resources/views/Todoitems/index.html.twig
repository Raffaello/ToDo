{% extends '::base.html.twig' %}

{% block body -%}
    <h1>Todoitems list</h1>

    <table class="records_list">
        <thead>
            <tr>
                <th>Userid</th>
                <th>Status</th>
                <th>Description</th>
                <th>Duedate</th>
                <th>Id</th>
                <th>Actions</th>
            </tr>
        </thead>
        <tbody>
        {% for entity in entities %}
            <tr>
                <td><a href="{{ path('todoitems_show', { 'id': entity.id }) }}">{{ entity.userId }}</a></td>
                <td>{{ entity.status }}</td>
                <td>{{ entity.description }}</td>
                <td>{% if entity.duedate %}{{ entity.duedate|date('Y-m-d H:i:s') }}{% endif %}</td>
                <td>{{ entity.id }}</td>
                <td>
                <ul>
                    <li>
                        <a href="{{ path('todoitems_show', { 'id': entity.id }) }}">show</a>
                    </li>
                    <li>
                        <a href="{{ path('todoitems_edit', { 'id': entity.id }) }}">edit</a>
                    </li>
                </ul>
                </td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

        <ul>
        <li>
            <a href="{{ path('todoitems_new') }}">
                Create a new entry
            </a>
        </li>
    </ul>
    {% endblock %}
